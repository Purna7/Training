name: first workflow 
on: workflow_dispatch

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome Message
              run: ech "First workflow"
            - name: List Files
              run: ls
            - name: Read File
              run: cat README.md


